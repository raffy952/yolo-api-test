# Copia questo file in ".env" (senza estensione) e personalizza i valori.
# Il file ".env" NON va mai committato su Git: contiene le vere credenziali.

# --- SQL Server ---
DB_USER=tecno
DB_PASSWORD=tecno
DB_SERVER=host.docker.internal
DB_PORT=1433
DB_NAME=YoloVisionDB

# --- Grafana ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme
